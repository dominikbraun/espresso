#
# Espresso CircleCi 2.1 Configuration
#
version: 2.1

executors:
  go-container:
    docker:
      - image: circleci/golang:1.13
        environment:
          CGO_ENABLED: 0
          G111MODULE: "on"

jobs:
  espresso-test:
    executor: go-container
    steps:
      - checkout
      - restore_cache:
          keys:
            - dependency-cache-v1-{{ .Branch }}-{{ checksum "go.mod" }}
            - dependency-cache-v1-{{ .Branch }}-
            - dependency-cache-v1-
            - repository-cache-v1-{{ .Branch }}-{{ .Revision }}
            - repository-cache-v1-{{ .Branch }}-
            - repository-cache-v1-
      - run: go mod download
      - save_cache:
          key: dependency-cache-v1-{{ .Branch }}-{{ checksum "go.mod" }}
          paths:
            - /go/pkg
      - save_cache:
          key: repository-cache-v1-{{ .Branch }}-{{ .Revision }}
          paths:
            - ".git"
      - run:
          name: Run all tests
          command: go test -v ./...

  espresso-release-build:
    executor: go-container
    steps:
      - checkout
      - restore_cache:
          keys:
            - dependency-cache-v1-{{ .Branch }}-{{ checksum "go.mod" }}
            - dependency-cache-v1-{{ .Branch }}-
            - dependency-cache-v1-
            - repository-cache-v1-{{ .Branch }}-{{ .Revision }}
            - repository-cache-v1-{{ .Branch }}-
            - repository-cache-v1-
      - run: go mod download
      - save_cache:
          key: dependency-cache-v1-{{ .Branch }}-{{ checksum "go.mod" }}
          paths:
            - /go/pkg
      - save_cache:
          key: repository-cache-v1-{{ .Branch }}-{{ .Revision }}
          paths:
            - ".git"
      - run: mkdir -p /tmp/artifacts
      - run:
          name: Build and pack Espresso for Linux
          command: |
            GOOS=linux GOARCH=amd64 go build -v -o .target/espresso cmd/main.go
            cp .target/espresso espresso
            tar -czf /tmp/artifacts/espresso-linux-amd64.tar.gz espresso
      - persist_to_workspace:
          root: /tmp/artifacts
          paths:
            - espresso-linux-amd64.tar.gz

  espresso-gh-release:
    docker:
      - image: cibuilds/github:0.10
    steps:
      - attach_workspace:
          at: /tmp/artifacts
      - run: mkdir artifacts && cp /tmp/artifacts/* artifacts/
      - run:
          name: Publish GitHub release
          command: |
            VERSION="0.0.0"
            ghr -t "${GITHUB_TOKEN}" \
                -u "${CIRCLE_PROJECT_USERNAME}" \
                -r "${CIRCLE_PROJECT_REPONAME}" \
                -c "${CIRCLE_SHA1}" \
                -delete "${VERSION}" \
                artifacts/

  espresso-docker-release:
    docker:
      - image: circleci/buildpack-deps:stretch
    steps:
      - checkout
      - run: echo ${DOCKER_PASS} | docker login --username ${DOCKER_USER} --password-stdin
      - run:
          name: Build the Docker image (Light)
          command: |
            VERSION="0.0.0"
            docker image build -t dominikbraun/espresso:${VERSION} .
      - run:
          name: Tag the Docker image (Light) as latest
          command: |
            docker image tag dominikbraun/espresso:${VERSION} dominikbraun/espresso:latest
      - run:
          name: Publish the Docker images (Light)
          command: |
            docker push dominikbraun/espresso:${VERSION}
            docker push dominikbraun/espresso:latest

workflows:
  version: 2
  espresso-ci:
    jobs:
      - espresso-test
      - espresso-release-build:
          requires:
            - espresso-test
          filters:
            branches:
              only:
                - circleci-project-setup
            tags:
              only: /^\d+\.\d+\.\d+$/
      - espresso-gh-release:
          requires:
            - espresso-release-build
      - espresso-docker-release:
          requires:
            - espresso-gh-release